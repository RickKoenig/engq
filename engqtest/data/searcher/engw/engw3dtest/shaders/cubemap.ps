uniform samplerCube uSampler;
uniform mediump float alphacutoff;      
     
varying highp vec3 vNormalCoord;

void main(void) {
	gl_FragColor = textureCube(uSampler,vNormalCoord);
	if (gl_FragColor.a < alphacutoff)
		discard;
}
